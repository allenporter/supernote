# supernote

The `supernote` library is a fork and slightly lighter dependency version of [supernote-tool](https://github.com/jya-dev/supernote-tool) that drops svg dependencies not found in some containers.

## Development

```
uv venv
source .venv/bin/activate
uv pip install -r requirements_dev.txt
```
